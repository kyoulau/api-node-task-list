import { config } from 'process';
const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  'migrations': resolve(__dirname, 'src', 'database', 'migrations'),
  'seeders': resolve(__dirname, 'src', 'database', 'seeders')
}